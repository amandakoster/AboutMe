'use strict';

var name = prompt('Hiya, what is your name?'){
alert('Welcome' + name + 'let's play a game.')
console.log("welcome" + name);
}



var question2 = seattleite('Do you think I am a native Seattleite?')
if (question2.toLowerCase() === 'yes' || answer.toLowerCase() === 'y'); {
  alert('For some reason the user things Amanda is a native Seattleite.')
  }
  else if (answer.toLowerCase() === 'no' || answer.toLowerCase() === 'n'); {
    alert ('You\'re correct! Amanda is not originally from Seattle.')
  }
  else {
    alert('Ya gotta play to get to the end. Humor me!');
  }



 var answer = prompt('Are you ready to rumble?');
 if (answer.toLowerCase() === 'yes' || answer.toLowerCase() === 'y') {
   // If the user answers yes, do the following.
   alert('The user said they are indeed ready to rumble.');
 } else if (answer.toLowerCase() === 'no' || answer.toLowerCase() === 'n') {
   /*
     If the user is not ready to rumble, do the following.
   */
   alert('The user is unfortunately not ready to rumble.');
 } else {
   // If the user can't read instructions properly, admonish them!
   alert('You didn\'t put in a proper answer!');
 }
